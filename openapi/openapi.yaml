openapi: 3.0.3
info:
  title: CIMA CRM - mod-media
  version: 1.0.0
  description: |
    Microservicio de media para avatares y documentos. Expone contratos directos
    del servicio; el gateway puede publicarlos como proxy sin fusionar specs.
servers:
  - url: http://localhost:3002
    description: mod-media directo (desarrollo)
tags:
  - name: Sistema
  - name: Documentacion
  - name: Avatares
  - name: Documentos
  - name: Correo
components:
  securitySchemes:
    GatewayIdentity:
      type: apiKey
      in: header
      name: X-User-Id
      description: Headers de identidad propagados por el gateway confiable.
  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
    Avatar:
      type: object
      properties:
        userId:
          type: string
        url:
          type: string
          format: uri
        objectKey:
          type: string
        updatedAt:
          type: string
          format: date-time
    DocumentUploadUrl:
      type: object
      required: [uploadUrl, objectKey, expiresInSeconds]
      properties:
        uploadUrl:
          type: string
          format: uri
        objectKey:
          type: string
        expiresInSeconds:
          type: integer
    DocumentAccess:
      type: object
      required: [url, expiresInSeconds]
      properties:
        url:
          type: string
          format: uri
        expiresInSeconds:
          type: integer
    DataEnvelope:
      type: object
      properties:
        data:
          type: object
security:
  - GatewayIdentity: []
paths:
  /api/v1/health:
    get:
      tags: [Sistema]
      summary: Health check
      security: []
      responses:
        "200":
          description: Servicio activo
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  service:
                    type: string
                    example: mod-media
  /api/v1/openapi.yaml:
    get:
      tags: [Documentacion]
      summary: Especificacion OpenAPI del servicio
      security: []
      responses:
        "200":
          description: Archivo OpenAPI YAML
          content:
            application/yaml:
              schema:
                type: string
  /api/v1/media/avatars:
    post:
      tags: [Avatares]
      summary: Subir avatar del usuario actual
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: Avatar actualizado
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/DataEnvelope"
  /api/v1/media/avatars/current:
    get:
      tags: [Avatares]
      summary: Obtener avatar vigente del usuario actual
      responses:
        "200":
          description: Avatar vigente
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/DataEnvelope"
  /api/v1/media/avatars/users:
    get:
      tags: [Avatares]
      summary: Obtener avatares vigentes de varios usuarios
      parameters:
        - in: query
          name: ids
          schema:
            type: string
          description: Lista de IDs separada por coma.
      responses:
        "200":
          description: Avatares por usuario
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/DataEnvelope"
  /api/v1/media/documents/upload-url:
    post:
      tags: [Documentos]
      summary: Generar URL prefirmada de carga para documentos
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [fileName, mimeType, sizeBytes]
              properties:
                fileName:
                  type: string
                mimeType:
                  type: string
                sizeBytes:
                  type: integer
      responses:
        "200":
          description: URL de carga generada
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DocumentUploadUrl"
  /api/v1/media/documents/confirm:
    post:
      tags: [Documentos]
      summary: Confirmar carga directa de documento en OCI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [objectKey, fileName, mimeType, sizeBytes]
              properties:
                objectKey:
                  type: string
                fileName:
                  type: string
                mimeType:
                  type: string
                sizeBytes:
                  type: integer
      responses:
        "201":
          description: Documento registrado
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/DataEnvelope"
  /api/v1/media/documents/access:
    get:
      tags: [Documentos]
      summary: Generar URL prefirmada de lectura para un documento
      parameters:
        - in: query
          name: objectKey
          required: true
          schema:
            type: string
        - in: query
          name: download
          schema:
            type: boolean
      responses:
        "200":
          description: URL de acceso generada
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DocumentAccess"
  /api/v1/media/documents:
    delete:
      tags: [Documentos]
      summary: Eliminar documento por objectKey
      parameters:
        - in: query
          name: objectKey
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Documento eliminado
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/DataEnvelope"
  /api/v1/emails/send:
    post:
      tags: [Correo]
      summary: Despachar correo electrónico (transaccional o marketing)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [to]
              properties:
                to:
                  oneOf:
                    - type: string
                      format: email
                    - type: array
                      items:
                        type: string
                        format: email
                subject:
                  type: string
                from:
                  type: string
                replyTo:
                  type: string
                  format: email
                cc:
                  type: array
                  items:
                    type: string
                    format: email
                bcc:
                  type: array
                  items:
                    type: string
                    format: email
                template:
                  type: object
                  properties:
                    name:
                      type: string
                      enum: [password_reset, client_invite, worker_invite, email_verify]
                    variables:
                      type: object
                content:
                  type: object
                  properties:
                    subject:
                      type: string
                    html:
                      type: string
                    text:
                      type: string
                sync:
                  type: boolean
                  default: false
                metadata:
                  type: object
      responses:
        "200":
          description: Correo enviado exitosamente (modo sincrónico)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  messageId:
                    type: string
                  status:
                    type: string
                    example: sent
        "202":
          description: Correo encolado exitosamente (modo asíncrono)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  messageId:
                    type: string
                  status:
                    type: string
                    example: queued
        "400":
          description: Parámetros inválidos
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/emails/send-template:
    post:
      tags: [Correo]
      summary: Despachar correo electrónico por plantilla abreviada
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [to, template]
              properties:
                to:
                  type: string
                  format: email
                template:
                  type: string
                  enum: [password_reset, client_invite, worker_invite, email_verify]
                variables:
                  type: object
                sync:
                  type: boolean
                  default: false
      responses:
        "200":
          description: Correo enviado exitosamente
        "202":
          description: Correo encolado exitosamente
        "400":
          description: Parámetros inválidos
